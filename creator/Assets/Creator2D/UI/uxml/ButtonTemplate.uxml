<UXML xmlns="UnityEngine.UIElements">
  <!-- Creates the button. -->
  <VisualElement class="unity-button toptool-button grow">      
    <!-- Adds a VisualElement child (corresponding to the button's icon). -->
    <VisualElement class="toptool-button-icon"/>   
  </VisualElement>
</UXML>